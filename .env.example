# git-commit-summarizer environment variables
# Copy to .env and fill in your values: cp .env.example .env

# Path to your LLM CLI binary (e.g., ollama, llm, aichat, custom wrapper)
export LLM_MAIN_ENTRY_BIN=llm

# Optional: enable holy word check before commits
# export ENABLE_HOLY_WORD_CHECK=1
